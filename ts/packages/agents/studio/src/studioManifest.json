{
  "emojiChar": "🎨",
  "description": "TypeAgent Studio agent — inspect Studio's environment, grammar collisions, and event stream (the AI / conversational presenter over the Studio engine)",
  "schema": {
    "description": "Read-only inspection of TypeAgent Studio: report Studio's environment and agent locations, list known cross-schema grammar collisions, and show recent Studio events. (More of the Studio loop — sandboxes, run, validate, author — arrives in later phases.)",
    "originalSchemaFile": "./studioSchema.ts",
    "schemaFile": "../dist/studioSchema.pas.json",
    "schemaType": "StudioActions"
  }
}
