// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.

// SCAFFOLDER TEMPLATE STUB — DO NOT IMPORT AT RUNTIME.
//
// This file is a placeholder schema module so the sibling `*.ts` scaffolder
// templates in this directory can be type-checked standalone by `tsc`. The
// real generated agent ships its own `<name>Schema.ts` next to its handler;
// at scaffold time `templateLoader` rewrites the import path so this stub
// is never referenced by emitted code.

// eslint-disable-next-line @typescript-eslint/naming-convention
export type __AgentName__Actions = {
    actionName: string;
    parameters?: Record<string, unknown>;
};
