{
  "emojiChar": "💬",
  "description": "Microsoft Teams integration via Microsoft Graph delegated APIs (user-scoped, corp-tenant compatible). Initial actions: sendChatMessage, replyToMessage, getJoinLink, findMessage, summarizeChatThread.",
  "defaultEnabled": true,
  "schema": {
    "description": "Teams agent actions",
    "originalSchemaFile": "./teamsSchema.ts",
    "schemaFile": "../dist/teamsSchema.pas.json",
    "grammarFile": "../dist/teamsSchema.ag.json",
    "schemaType": "TeamsActions"
  }
}