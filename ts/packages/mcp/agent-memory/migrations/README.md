# Database Migrations

SQLite migrations will be added with the repository milestone. This directory
is included in the packed MCP artifact so migrations can be loaded without the
TypeAgent repository.
