{
  "name": "plan-validation-demo",
  "version": "0.0.1",
  "description": "Example project for plan-validated agent execution. Launches an MCP validation server with live dashboard and scopes Claude Code to a small landing-page scenario.",
  "license": "MIT",
  "author": "Microsoft",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "node setup.mjs",
    "clean": "node setup.mjs --clean",
    "reset": "node setup.mjs --clean && node setup.mjs"
  },
  "devDependencies": {
    "mcp-plan-validation": "workspace:*",
    "tsx": "^4.21.0"
  }
}
