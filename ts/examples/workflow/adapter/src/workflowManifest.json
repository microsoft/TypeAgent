{
  "emojiChar": "🔄",
  "description": "Run automated developer workflows via chat",
  "schema": {
    "description": "Workflow agent: each discovered workflow is a distinct action with typed parameters",
    "originalSchemaFile": "./workflowSchema.ts",
    "schemaFile": "../dist/workflowSchema.pas.json",
    "schemaType": "WorkflowAction"
  }
}
