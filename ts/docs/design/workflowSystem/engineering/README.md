# Engineering

Status: Placeholder.

This folder will hold engineering-side concerns that cut across components:
package layout, build plan, milestones, integration with the TypeAgent
dispatcher, test strategy.

The earlier engineering plan (goals, non-goals, package layout, milestones,
task plugin API sketch) lives in [../archive/plan.md](../archive/plan.md).
Live content from there will be distilled into this folder when concrete
engineering work resumes:

- A current `milestones.md` or `plan.md` (goals, non-goals, milestones).
- An `integration.md` (dispatcher adapter, agent registration). Today this
  is sketched in archive/plan.md §4.
- A `packages.md` (workspace layout under `packages/` and `examples/`).

The task plugin API sketch in archive/plan.md §6 belongs to a future
`tasks/` component, not here.
