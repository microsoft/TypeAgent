# `@copilot fix` — Status

Tracks progress for the plan in [PLAN.md](./PLAN.md). Update as phases land.

Status: **In progress** (implementation landed; verification pending a full monorepo build).

## Phase checklist

- [x] **Phase 1 — Command + conversation assembly (dispatcher)**
  - [x] `FixWithCopilotCommandHandler` added to `copilotCommandHandlers.ts`; `fix` subcommand registered.
  - [x] Flags: `--mode`, `--no-screenshot`, `--dev-captures`, (`--target` reserved); optional trailing instructions.
  - [x] `conversation.json` written from `displayLog.getEntries()`.
  - [x] Dev-capture selection (developer mode + persistent session, correlated by `requestId`).
  - [x] Prompt (`query`) composition.
- [x] **Phase 2 — Copilot-launch action (code agent + Coda)**
  - [x] `EditorActionLaunchCopilotChat` added to `editorCodeActionsSchema.ts` (compiled `.pas.json` is a gitignored build artifact regenerated by `asc:editor`).
  - [x] `launchCopilotChat` handler in Coda → `workbench.action.chat.open`.
  - [x] Older-build capability guard / fallback (embeds attachment paths in the query).
- [x] **Phase 3 — Orchestrate + confirm**
  - [x] `askYesNoWithContext` confirmation.
  - [x] `executeActions` dispatch of `code.code-editor` / `launchCopilotChat`.
  - [x] Not-connected error handling (pre-check `isActionActive`; guidance on failure).
- [ ] **Phase 4 — Verify**
  - [ ] Build + `prettier:fix`.
  - [ ] Unit tests (`agent-dispatcher`, `coda`).
  - [ ] Manual end-to-end in the VS Code shell.

## Notes / decisions log

- Conversation source is the **raw display log** (`displayLog.getEntries()`), not `conversationMemory`.
- Developer-mode `dev-captures` attached as supplementary JSON when available.
- Screenshot via native `attachScreenshot: true`; conversation/captures via `attachFiles`.
- Execution via the `code` agent + Coda bridge (frontend-agnostic); native Copilot in agent mode; pre-fill (no auto-submit); manual trigger.
