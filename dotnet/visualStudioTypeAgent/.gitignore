# All other patterns (bin/, obj/, *.user, etc.) are covered by the
# repo-root .gitignore — keep this file scoped to project-specific items.

# WebView2 bundled content (generated by xcopy from
# ../../ts/packages/agents/visualStudio/host/webview/dist).
webview-content/*
!webview-content/.gitkeep
